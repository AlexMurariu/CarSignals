import { mainColor, secondaryColor } from './colorConstants';
import { predictionNames, predictionTexts } from './predictionConstants';

export {
    predictionNames,
    predictionTexts,
    mainColor,
    secondaryColor
}