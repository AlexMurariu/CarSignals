import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        height: '100%'
    },
    card: {
        height: 300,
        position: 'absolute',
        top: "35%"
    },
    inputs: {
        marginVertical: 5
    },
    buttons: {
        marginVertical: 10
    }
})