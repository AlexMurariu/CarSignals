import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        display: "flex",
        alignItems: "center"
    },
    iconContainer: {
        marginTop: 80
    },
    closeButton: {
        margin: 15,
        position: "absolute",
        top: 0,
        right: 0
    }
})